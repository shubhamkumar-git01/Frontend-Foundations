// 02: Operators and Logic
let x = 10;
let y = "10";

// == checks value, === checks value AND type (ALWAYS USE ===)
console.log(x == y); // true
console.log(x === y); // false

// Ternary Operator
let age = 20;
let status = (age >= 18) ? "Adult" : "Minor";
console.log(status);
