// 10: HTML5 Canvas via JS
// This requires an HTML file with <canvas id="board"></canvas>

/*
const canvas = document.getElementById('board');
const ctx = canvas.getContext('2d');

// Draw a rectangle
ctx.fillStyle = 'blue';
ctx.fillRect(10, 10, 100, 50);

// Draw a line
ctx.beginPath();
ctx.moveTo(0, 0);
ctx.lineTo(200, 100);
ctx.stroke();
*/
console.log("Canvas API requires DOM context. See comments for code.");
